---
interface Props {
    link: string;
    icon: string;
    desc: string;
}
const { link, icon, desc } = Astro.props;
---

<a class="text-3xl" aria-label="link" target="_blank" href={link} title={desc}
    ><i class={icon}></i></a
>
